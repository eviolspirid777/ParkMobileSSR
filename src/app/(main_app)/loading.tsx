import { LoadingComponent } from "@/Shared/Components/Loading/Loading";

export default function Loading() {
  return (
    <div
      style={{
        height: "70vh",
      }}
    >
      <LoadingComponent />
    </div>
  );
}
